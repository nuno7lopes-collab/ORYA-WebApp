"use client"; import { useCallback, useEffect, useMemo, useState } from"react";
import Link from"next/link";
import { useSearchParams } from"next/navigation";
import { buildOrgHref } from"@/lib/organizationIdUtils";
import { mapPaymentGateUiState, parseApiError } from"@/lib/payments/paymentGateUi";
import { cn } from"@/lib/utils";
import StoreActivationCard from"@/components/store/StoreActivationCard";
import StoreOverviewPanel from"@/components/store/StoreOverviewPanel";
import StoreProductsPanel from"@/components/store/StoreProductsPanel";
import StoreCategoriesPanel from"@/components/store/StoreCategoriesPanel";
import StoreProductImagesPanel from"@/components/store/StoreProductImagesPanel";
import StoreProductVariantsPanel from"@/components/store/StoreProductVariantsPanel";
import StoreProductOptionsPanel from"@/components/store/StoreProductOptionsPanel";
import StoreProductOptionValuesPanel from"@/components/store/StoreProductOptionValuesPanel";
import StoreProductDigitalAssetsPanel from"@/components/store/StoreProductDigitalAssetsPanel";
import StoreOrdersPanel from"@/components/store/StoreOrdersPanel";
import StoreShippingSettingsPanel from"@/components/store/StoreShippingSettingsPanel";
import StoreShippingZonesPanel from"@/components/store/StoreShippingZonesPanel";
import StoreShippingMethodsPanel from"@/components/store/StoreShippingMethodsPanel";
import StoreShippingTiersPanel from"@/components/store/StoreShippingTiersPanel";
import StoreBundlesPanel from"@/components/store/StoreBundlesPanel";
import StoreBundleItemsPanel from"@/components/store/StoreBundleItemsPanel"; type StoreSnapshot = { id: number; status: string; catalogLocked: boolean; checkoutEnabled: boolean; showOnProfile: boolean; publicProductsCount?: number; createdAt: string; updatedAt: string; }; type StorePreviewCounts = { total: number; public: number; draft: number; }; type OrgStoreToolClientProps = { orgId: number; }; type StoreUiError = { code: string | null; message: string; ctaHref: string | null; ctaLabel: string | null; }; const STORE_VIEWS = ["overview","catalog","orders","shipping","marketing"] as const;
type StoreView = (typeof STORE_VIEWS)[number]; const VIEW_SUBNAV = { overview: [] as const, catalog: [ { id:"products", label:"Produtos" }, { id:"categories", label:"Categorias" }, { id:"images", label:"Imagens" }, { id:"variants", label:"Variantes" }, { id:"options", label:"Opções" }, { id:"option-values", label:"Valores" }, { id:"digital-assets", label:"Digitais" }, ] as const, orders: [{ id:"orders", label:"Lista" }] as const, shipping: [ { id:"settings", label:"Configuração" }, { id:"zones", label:"Zonas" }, { id:"methods", label:"Métodos" }, { id:"tiers", label:"Tabelas" }, ] as const, marketing: [ { id:"bundles", label:"Bundles" }, { id:"bundle-items", label:"Itens bundle" }, ] as const, } as const; const DEFAULT_SUB_BY_VIEW: Record<StoreView, string | undefined> = { overview: undefined, catalog:"products", orders:"orders", shipping:"settings", marketing:"bundles", }; function parseStoreView(raw: string | null): StoreView { if (!raw) return"overview"; const normalized = raw.trim().toLowerCase(); return STORE_VIEWS.includes(normalized as StoreView) ? (normalized as StoreView) :"overview"; } function parseStoreSub(view: StoreView, raw: string | null) { const options = VIEW_SUBNAV[view]; if (options.length === 0) return undefined; const normalized = raw?.trim().toLowerCase(); const found = normalized ? options.find((entry) => entry.id === normalized) : null; if (found) return found.id; return DEFAULT_SUB_BY_VIEW[view]; } function unwrapEnvelope(payload: unknown) { if (!payload || typeof payload !=="object") return payload; const asRecord = payload as Record<string, unknown>; if (asRecord.data && typeof asRecord.data ==="object") return asRecord.data; if (asRecord.result && typeof asRecord.result ==="object") return asRecord.result; return payload; } function resolveErrorCode(payload: unknown, fallbackStatus: number) { const unwrapped = unwrapEnvelope(payload); const topLevel = payload && typeof payload ==="object" ? (payload as Record<string, unknown>) : null; const body = unwrapped && typeof unwrapped ==="object" ? (unwrapped as Record<string, unknown>) : null; const candidate = (body?.errorCode as string | undefined) ?? (topLevel?.errorCode as string | undefined) ?? (body?.error as string | undefined) ?? (topLevel?.error as string | undefined); if (typeof candidate ==="string" && candidate.trim()) return candidate.trim(); return`HTTP_${fallbackStatus}`; } function formatStoreError(errorCode: string | null) { if (!errorCode) return null; if (errorCode.toUpperCase() ==="LOJA_UNAVAILABLE") { return"Ferramenta Loja inativa para esta organização."; } if (errorCode ==="OFFICIAL_EMAIL_REQUIRED") { return"Configura o email oficial da organização antes de usar a Loja."; } if (errorCode ==="OFFICIAL_EMAIL_NOT_VERIFIED") { return"Confirma o email oficial da organização para usar a Loja."; } if (errorCode ==="UNAUTHENTICATED") { return"Sessão expirada. Volta a iniciar sessão."; } if (errorCode.startsWith("HTTP_")) { return"Não foi possível carregar a Loja. Tenta novamente."; } return errorCode; } function buildStoreUiError(payload: unknown, fallbackStatus: number, orgId: number): StoreUiError { const parsedError = parseApiError(payload,"Não foi possível carregar a Loja. Tenta novamente."); const paymentUi = mapPaymentGateUiState({ organizationId: orgId, errorCode: parsedError.errorCode, message: parsedError.message, details: parsedError.details, }); if (paymentUi.ctaHref || paymentUi.ctaLabel) { return { code: parsedError.errorCode, message: paymentUi.message, ctaHref: paymentUi.ctaHref, ctaLabel: paymentUi.ctaLabel, }; } const errorCode = parsedError.errorCode ?? resolveErrorCode(payload, fallbackStatus); return { code: errorCode, message: formatStoreError(errorCode) ?? parsedError.message, ctaHref: null, ctaLabel: null, }; } export default function OrgStoreToolClient({ orgId }: OrgStoreToolClientProps) { const searchParams = useSearchParams(); const view = parseStoreView(searchParams?.get("view") ?? null); const sub = parseStoreSub(view, searchParams?.get("sub") ?? null); const subItems = VIEW_SUBNAV[view]; const storeEnabled = true; const endpoints = useMemo(() => { const base =`/api/org/${orgId}/store`; return { base, overview:`${base}/overview`, categories:`${base}/categories`, products:`${base}/products`, orders:`${base}/orders`, shippingSettings:`${base}/shipping/settings`, shippingZones:`${base}/shipping/zones`, bundles:`${base}/bundles`, }; }, [orgId]); const [store, setStore] = useState<StoreSnapshot | null>(null); const [previewCounts, setPreviewCounts] = useState<StorePreviewCounts | null>(null); const [loadingStore, setLoadingStore] = useState(false); const [storeError, setStoreError] = useState<StoreUiError | null>(null); const loadStore = useCallback(async () => { setLoadingStore(true); setStoreError(null); setPreviewCounts(null); try { const res = await fetch(endpoints.base, { cache:"no-store" }); const payload = await res.json().catch(() => null); const unwrapped = unwrapEnvelope(payload); const topLevel = payload && typeof payload ==="object" ? (payload as Record<string, unknown>) : null; const body = unwrapped && typeof unwrapped ==="object" ? (unwrapped as Record<string, unknown>) : null; const hasErrorFlag = topLevel?.ok === false || body?.ok === false; if (!res.ok || hasErrorFlag) { setStoreError(buildStoreUiError(payload, res.status, orgId)); return; } setStore((body?.store ?? topLevel?.store ?? null) as StoreSnapshot | null); try { const previewRes = await fetch(`/api/org/${orgId}/store/preview`, { cache:"no-store" }); const previewPayload = await previewRes.json().catch(() => null); const previewUnwrapped = unwrapEnvelope(previewPayload); const previewTopLevel = previewPayload && typeof previewPayload ==="object" ? (previewPayload as Record<string, unknown>) : null; const previewBody = previewUnwrapped && typeof previewUnwrapped ==="object" ? (previewUnwrapped as Record<string, unknown>) : null; const previewHasError = previewTopLevel?.ok === false || previewBody?.ok === false; if (!previewRes.ok || previewHasError) { return; } const counts = previewBody?.counts as Record<string, unknown> | undefined; if (counts && typeof counts.total ==="number" && typeof counts.public ==="number" && typeof counts.draft ==="number") { setPreviewCounts({ total: counts.total, public: counts.public, draft: counts.draft, }); } } catch { } } catch (err) { setStoreError({ code: null, message: err instanceof Error ? err.message :"Erro inesperado.", ctaHref: null, ctaLabel: null, }); } finally { setLoadingStore(false); } }, [endpoints.base, orgId]); useEffect(() => { void loadStore(); }, [loadStore]); const buildStoreHref = useCallback((nextView: StoreView, nextSub?: string) => { const query: Record<string, string> = { view: nextView }; const resolvedSub = nextSub ?? DEFAULT_SUB_BY_VIEW[nextView]; if (resolvedSub) { query.sub = resolvedSub; } return buildOrgHref(orgId,"/store", query); }, [orgId],); const storeLocked = Boolean(store?.catalogLocked); const hasStore = Boolean(store); const storeErrorMessage = storeError?.message ?? null; const renderPanel = () => { if (!hasStore) return null; if (view ==="overview") { return <StoreOverviewPanel endpoint={endpoints.overview} />; } if (view ==="catalog") { if (sub ==="categories") { return (<StoreCategoriesPanel endpointBase={endpoints.categories} storeLocked={storeLocked} storeEnabled={storeEnabled} />); } if (sub ==="images") { return (<StoreProductImagesPanel productsEndpoint={endpoints.products} storeLocked={storeLocked} storeEnabled={storeEnabled} organizationId={orgId} />); } if (sub ==="variants") { return (<StoreProductVariantsPanel productsEndpoint={endpoints.products} storeLocked={storeLocked} storeEnabled={storeEnabled} />); } if (sub ==="options") { return (<StoreProductOptionsPanel productsEndpoint={endpoints.products} storeLocked={storeLocked} storeEnabled={storeEnabled} />); } if (sub ==="option-values") { return (<StoreProductOptionValuesPanel productsEndpoint={endpoints.products} storeLocked={storeLocked} storeEnabled={storeEnabled} />); } if (sub ==="digital-assets") { return (<StoreProductDigitalAssetsPanel productsEndpoint={endpoints.products} storeLocked={storeLocked} storeEnabled={storeEnabled} />); } return (<StoreProductsPanel endpointBase={endpoints.products} categoriesEndpoint={endpoints.categories} storeLocked={storeLocked} storeEnabled={storeEnabled} organizationId={orgId} />); } if (view ==="orders") { return <StoreOrdersPanel endpointBase={endpoints.orders} storeEnabled={storeEnabled} />; } if (view ==="shipping") { if (sub ==="zones") { return <StoreShippingZonesPanel endpointBase={endpoints.shippingZones} storeEnabled={storeEnabled} />; } if (sub ==="methods") { return <StoreShippingMethodsPanel zonesEndpoint={endpoints.shippingZones} storeEnabled={storeEnabled} />; } if (sub ==="tiers") { return <StoreShippingTiersPanel zonesEndpoint={endpoints.shippingZones} storeEnabled={storeEnabled} />; } return <StoreShippingSettingsPanel endpoint={endpoints.shippingSettings} storeEnabled={storeEnabled} />; } if (view ==="marketing") { if (sub ==="bundle-items") { return (<StoreBundleItemsPanel bundlesEndpoint={endpoints.bundles} productsEndpoint={endpoints.products} storeLocked={storeLocked} storeEnabled={storeEnabled} />); } return (<StoreBundlesPanel endpointBase={endpoints.bundles} storeLocked={storeLocked} storeEnabled={storeEnabled} />); } return null; }; return (<section className="space-y-4 text-white"> {storeError ? (<div className="rounded-2xl border border-rose-500/35 bg-rose-500/10 px-4 py-3 text-sm text-rose-100"> <p>{storeErrorMessage}</p> {storeError.ctaHref && storeError.ctaLabel ? (<a href={storeError.ctaHref} className="mt-3 inline-flex rounded-lg border border-rose-200/50 bg-rose-200/20 px-3 py-1.5 text-xs font-semibold text-rose-50 transition hover:bg-rose-200/35" > {storeError.ctaLabel} </a>) : null} </div>) : null} {view ==="overview" || !hasStore ? (<StoreActivationCard organizationId={orgId} title="Loja da organização" description="Controla publicação da loja no perfil público." endpoint={endpoints.base} storeEnabled={storeEnabled} initialStore={store} onStoreChange={setStore} />) : null} {view ==="overview" && previewCounts ? (<div className="rounded-2xl border border-cyan-400/30 bg-cyan-500/10 px-4 py-3 text-sm text-cyan-100"> Preview da loja: {previewCounts.public} públicos, {previewCounts.draft} draft,{""} {previewCounts.total} total. </div>) : null} {!hasStore ? (<div className="rounded-2xl border border-white/12 bg-black/35 px-4 py-4 text-sm text-white/75"> {loadingStore ?"A carregar estado da loja..." :"Sem dados da loja de momento."} </div>) : (<> {subItems.length > 1 ? (<nav className="rounded-2xl border border-white/12 bg-white/5 px-2 py-2"> <div className="orya-scrollbar-hide flex items-center gap-1 overflow-x-auto"> {subItems.map((item) => { const active = sub === item.id || (!sub && item.id === DEFAULT_SUB_BY_VIEW[view]); return (<Link key={item.id} href={buildStoreHref(view, item.id)} className={cn("inline-flex items-center rounded-full px-3 py-1.5 text-[12px] font-semibold whitespace-nowrap transition", active ?"bg-white/15 text-white" :"text-white/70 hover:bg-white/10",)} aria-current={active ?"page" : undefined} > {item.label} </Link>); })} </div> </nav>) : null} {renderPanel()} </>)} </section>); }
